import { DropdownMenu, DropdownMenuTrigger } from "../ui/dropdown-menu";

const TopBar = () => {
  return (
    <header className="fixed top-0 left-0 w-full z-50 p-3 h-12 shadow-md border-b">
      <div></div>
    </header>
  );
};

export default TopBar;
